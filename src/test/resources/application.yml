spring:
  application:
    name: device-manager
  cloud:
    config:
      enabled: false
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
server:
  port: 6060

graphiql:
  mapping: /graphql
  enabled: true
  cdn:
    enabled: false
